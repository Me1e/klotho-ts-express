{
  "name": "klotho-ts-express",
  "scripts": {
    "serve": "npx tsc && node dist/index.js",
    "up": "npx tsc && klotho . --app klotho-ts-express -p aws && cd compiled && pulumi config set aws:region ap-northeast-2 -s 'klotho-ts-express' && npm i && pulumi up --yes && cd ..",
    "down": "cd compiled && pulumi destroy --yes && cd .."
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "typescript": "^5.0.4"
  }
}
